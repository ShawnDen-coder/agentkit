{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"AgentKit","text":""},{"location":"#overview","title":"Overview","text":"<p>A transferable, embeddable agent framework for data-driven tools (BI dashboards now; DCC hosts like Maya/UE/PySide later). Modeled on OpenBB Workspace: two narrow waists (a frozen <code>ComponentAdapter</code> contract + a frozen SSE protocol) carry the portability; domain semantics live in swappable profile packages (<code>agentkit-bi</code>, future <code>agentkit-dcc</code>).</p> <ul> <li>Core is domain-neutral (only <code>pydantic</code>); langchain enters only at the   <code>LlmClient</code> layer (M2), never the contracts.</li> <li>Orchestration is stateless (hand-rolled; state lives in <code>request.messages</code>).</li> <li>Option B: data/skill/MCP are backend-sync calls; the FunctionCall loop is reserved   for frontend UI actions only.</li> </ul> <p>See <code>agentkit-architecture.md</code> for the full design and <code>docs/contracts.md</code> for the M1 frozen-contract surface.</p>"},{"location":"#workspace-layout","title":"Workspace layout","text":"<p>uv workspace; packages live under <code>packages/</code>.</p> Package Description Milestone <code>agentkit-core</code> Domain-neutral protocol core: SSE 6 events, <code>Component</code>/<code>ComponentSchema</code> envelope, <code>ComponentAdapter</code> Protocol, verb catalogue, testing DSL M1 \u2705 <code>agentkit-bi</code> BI profile: <code>BiSemanticModel</code>, <code>Widget</code>, <code>WidgetData</code>, <code>ChartArtifact</code>, <code>BiAdapter</code> base, BI prompt builder M1 \u2705"},{"location":"#development","title":"Development","text":"<pre><code>uvx --from rust-just just init      # sync deps + install pre-commit hooks\nuvx --from rust-just just lint      # ruff check --fix + format\nuvx --from rust-just just test      # pytest (dev Python version)\nuvx --from rust-just just test-all  # pytest across the configured Python range\n</code></pre>"},{"location":"contracts/","title":"AgentKit Frozen Contracts","text":"<p>The two narrow waists + supporting contracts. This document enumerates the frozen surface and the versioning policy. Changes to anything listed here require a version bump (see Versioning) and an update to this file + <code>agentkit-architecture.md</code>.</p> <p><code>PROTOCOL_VERSION = \"0.2.0\"</code> (defined in <code>agentkit_core</code>).</p> <p>0.2.0 (M2) changes (additive, backward-compatible): - <code>SessionContext.auth_token: SecretStr | None</code> (<code>exclude=True</code>) - BI auth token for   Option B backend-fetch. Transported via HTTP <code>Authorization</code> header (FastAPI layer   extracts + injects), never the request body; never serialized into <code>model_dump()</code> /   JSON / logs. Closes the \u00a75.1-vs-\u00a710 gap. - <code>LlmClient</code> Protocol tightened: <code>call -&gt; LlmResponse</code>, <code>stream -&gt; AsyncIterator[LlmChunk]</code>,   param <code>functions</code> -&gt; <code>tools: list[ToolDef] | None</code>. New provider-neutral types in   <code>agentkit_core.llm</code>: <code>ToolDef</code>, <code>LlmChunk</code>, <code>LlmToolCall</code>, <code>LlmResponse</code>. Lets the   orchestrator route tool calls without depending on any LLM SDK.</p> <p>0.1.0 (M1): initial freeze - the 6 SSE events, <code>QueryRequest</code>/<code>Message</code>/<code>SessionContext</code>, <code>Component</code>/<code>ComponentSchema</code> envelope, <code>ComponentAdapter</code> Protocol, 11-verb catalogue, testing DSL.</p>"},{"location":"contracts/#narrow-waist-2-sse-protocol","title":"Narrow waist \u2461 \u2014 SSE protocol","text":"<p>The wire contract between frontend and backend (Option B). Consumed by FastAPI <code>EventSourceResponse</code>.</p>"},{"location":"contracts/#the-6-events","title":"The 6 events","text":"Event class Wire <code>event</code> name Payload <code>CopilotMessageChunk</code> <code>copilotMessageChunk</code> <code>text</code>, <code>message_id?</code> <code>CopilotStatusUpdate</code> <code>copilotStatusUpdate</code> <code>status</code>, <code>label?</code> <code>CopilotMessageArtifact</code> <code>copilotMessageArtifact</code> <code>artifact</code> (envelope), <code>message_id?</code> <code>CopilotFunctionCall</code> <code>copilotFunctionCall</code> <code>name</code>, <code>arguments</code> (frontend UI action only) <code>CopilotCitationCollection</code> <code>copilotCitationCollection</code> <code>citations[]</code> <code>CopilotPromptSuggestions</code> <code>copilotPromptSuggestions</code> <code>suggestions[]</code>"},{"location":"contracts/#serialization-contract","title":"Serialization contract","text":"<p><code>BaseSSE.to_sse() -&gt; {\"event\": str, \"data\": &lt;json string&gt;}</code>. The <code>event</code> name is a class-level discriminator (not duplicated inside <code>data</code>). <code>data</code> is <code>model_dump_json(by_alias=True, exclude_none=True)</code> of the payload. SSE events use <code>extra=\"forbid\"</code> (strict).</p>"},{"location":"contracts/#request-envelope","title":"Request envelope","text":"<ul> <li><code>QueryRequest</code>: <code>messages[]</code>, <code>session_context</code>, <code>protocol_version</code> (default   <code>PROTOCOL_VERSION</code>), <code>tools?</code>, <code>features?</code>, <code>workspace_options?</code>. Stateless: all   state lives in <code>messages</code>.</li> <li><code>Message</code>: <code>role</code> \u2208 <code>{human, tool, assistant, system}</code> drives the state machine   (\u00a76.3). <code>role=tool</code> only for frontend UI-action results (carries <code>name</code> + <code>data</code>).</li> <li><code>SessionContext</code>: <code>user_identity</code>, <code>user_permissions[]</code>, <code>workspace_id</code>, <code>trace_id</code>,   <code>auth_token: SecretStr | None</code> (0.2.0, <code>exclude=True</code>). The auth token travels via the   HTTP <code>Authorization</code> header (extracted + injected by the FastAPI layer), NOT the body;   it is never serialized (excluded from <code>model_dump()</code> / JSON / logs). Adapters read it   via <code>ctx.auth_token.get_secret_value()</code>.</li> </ul>"},{"location":"contracts/#narrow-waist-1-componentadapter-contract","title":"Narrow waist \u2460 \u2014 ComponentAdapter contract","text":"<p>The contract a host-tool adapter implements. Discovered via the <code>agentkit.adapters</code> entry-point group; runtime never statically depends on adapters.</p>"},{"location":"contracts/#componentadapter-protocol-agentkit_coreprotocols","title":"ComponentAdapter Protocol (<code>agentkit_core.protocols</code>)","text":"Member Kind <code>origin: str</code> attribute <code>capabilities: AdapterCapabilities</code> attribute <code>async list_components(ctx) -&gt; list[Component]</code> method (verb: get_catalog) <code>async get_component(ctx, component_id) -&gt; Component</code> method <code>async get_component_data(ctx, component, input_args) -&gt; ComponentData</code> method (verb: get_component_data) <code>async refine_component(ctx, component, refinement) -&gt; ComponentData</code> method (verb: refine_component) <code>async get_semantic_model(ctx, component) -&gt; ComponentSchema</code> method (verb: get_semantic_model)"},{"location":"contracts/#domain-neutral-abstractions-agentkit_coremodels","title":"Domain-neutral abstractions (<code>agentkit_core.models</code>)","text":"<ul> <li><code>Component</code>: <code>component_id</code>, <code>origin</code>, <code>name</code>, <code>params[]</code>, <code>capabilities</code>,   <code>schema_</code> (the schema envelope).</li> <li><code>ComponentSchema</code>: polymorphic envelope (<code>kind: str</code>, <code>extra=\"allow\"</code>). Core never   parses content. Profiles subclass (<code>BiSemanticModel</code>, future <code>DccSchema</code>).</li> <li><code>ComponentData</code> / <code>Refinement</code>: same polymorphic-envelope pattern.</li> <li><code>AdapterCapabilities</code> / <code>ComponentCapabilities</code>: base + <code>extra=\"allow\"</code>; profiles   extend. Core flags: <code>supports_catalog</code>, <code>supports_selection</code>, <code>max_concurrent_fetch</code>.</li> <li><code>Field</code>: shared generic field (BI dimensions/measures, future DCC attributes).</li> </ul>"},{"location":"contracts/#the-envelope-mechanism-how-core-doesnt-parse-content-actually-works","title":"The envelope mechanism (how \"core doesn't parse content\" actually works)","text":"<p>Profile schemas (e.g. <code>BiSemanticModel</code>) are subclasses of the core envelope (<code>ComponentSchema</code>) with typed fields. Core fields that hold them use <code>SerializeAsAny[ComponentSchema]</code> so subclass fields survive JSON round-trip:</p> <ul> <li>Adapter constructs <code>BiSemanticModel(...)</code> and sets <code>component.schema_</code>.</li> <li><code>Component.model_dump_json()</code> serializes the BI fields (via <code>SerializeAsAny</code>).</li> <li><code>Component.model_validate_json(...)</code> reparses <code>schema_</code> as the opaque base   (<code>ComponentSchema</code>); BI fields are preserved via <code>extra=\"allow\"</code>.</li> <li>BI code downcasts back: <code>BiSemanticModel.model_validate(component.schema_.model_dump())</code>,   or via <code>Widget.semantic_model</code> / <code>BiAdapter.as_bi_schema</code>.</li> </ul>"},{"location":"contracts/#schema_-attribute-name","title":"<code>schema_</code> attribute name","text":"<p>The Python attribute is <code>schema_</code> (trailing underscore) to avoid shadowing <code>BaseModel.schema</code>; it serializes to the wire key <code>schema</code> (alias). This is the only aliased field in M1.</p>"},{"location":"contracts/#verb-catalogue-11","title":"Verb catalogue (11)","text":"<p>Per Option B (\u00a7D.4), verbs split by execution location:</p> <ul> <li>Backend sync (7): <code>get_component_data</code>, <code>refine_component</code>, <code>get_catalog</code>,   <code>get_selection</code>, <code>get_semantic_model</code>, <code>get_skill_content</code>, <code>execute_tool</code>.</li> <li>Frontend FunctionCall (4): <code>add_component_to_dashboard</code>,   <code>update_component_in_dashboard</code>, <code>manage_navigation_bar</code>, <code>assign_tasks_to_agents</code>.</li> </ul> <p>M1 freezes the catalogue (names + input schemas + category) in <code>STANDARD_VERBS</code>. The live <code>VerbRegistry</code> (binding handlers) is wired in M2. BI aliases (<code>get_widget_data</code> \u2192 <code>get_component_data</code>, etc.) and <code>export_artifact</code> are declared in <code>agentkit_bi</code>.</p>"},{"location":"contracts/#authentication-authorization-seam-user-implemented","title":"Authentication &amp; authorization seam (user-implemented)","text":"<p>Auth runs before a request enters the SSE/orchestrator path, so it is not part of either narrow waist and does not bump <code>PROTOCOL_VERSION</code>. The contracts live in <code>agentkit_core.auth</code> and are transport-agnostic (no HTTP type) so one implementation serves both the web profile (FastAPI) and the DCC profile (PySide/Maya host session).</p> <p>Users plug in their own mechanism (JWT, OAuth2, API-key, mTLS, studio SSO, ...) by implementing two Protocols:</p> <ul> <li><code>Authenticator.authenticate(context: AuthContext) -&gt; Principal</code> (async) - verifies   the caller and returns a <code>Principal</code> (<code>user_identity</code>, <code>user_permissions</code>, delegated   <code>auth_token: SecretStr</code>, <code>workspace_id</code>, <code>metadata</code>). Raise <code>AuthenticationError</code> (-&gt; 401).</li> <li><code>Authorizer.authorize(principal, action: AuthzAction) -&gt; None</code> (async) - per-verb   RLS/scope check; return to allow, raise <code>AuthorizationError</code> (-&gt; 403). <code>AuthzAction</code>   carries <code>verb</code> + <code>component_id</code> + <code>args</code> for field/row-level decisions.</li> </ul> <p>Supporting types: <code>AuthContext</code> (headers/query/cookies/peer bag; <code>header()</code> is case-insensitive), <code>Principal</code> (frozen; <code>auth_token</code> is <code>exclude=True</code> so it is never serialized), <code>AuthzAction</code> (frozen). Trivial dev/test impls: <code>AllowAllAuthorizer</code>, <code>DenyAllAuthorizer</code> (a header-trusting <code>Authenticator</code> ships in <code>agentkit-mock-app</code>).</p> <p>The app layer bridges a verified <code>Principal</code> onto the wire <code>SessionContext</code> via <code>session_from_principal(principal, *, trace_id, workspace_id=None)</code>. <code>SessionContext</code> is unchanged (still the RLS passthrough vehicle); the \u00a710 invariants - no service account, short-lived/unlogged/uncached token - are enforced by the implementations, not the seam.</p>"},{"location":"contracts/#what-is-not-yet-implemented-deferred","title":"What is NOT yet implemented (deferred)","text":"<ul> <li><code>Orchestrator</code> / <code>LlmClient</code> - Protocol only in 0.1.0; <code>LlmClient</code> tightened in 0.2.0.   <code>LangChainLlmClient</code> landed in <code>agentkit-llm-langchain</code> (M2); <code>StatelessOrchestrator</code>   (in <code>agentkit-runtime</code>) still pending.</li> <li>Live <code>VerbRegistry</code>, FastAPI app, SSE endpoint - M2. Packages <code>agentkit-runtime</code>,   <code>agentkit-mock-app</code>, <code>agentkit-cli</code> are scaffolded (M2 in progress); the auth seam   (<code>Authenticator</code>/<code>Authorizer</code>/<code>Principal</code>/<code>AuthContext</code>/<code>AuthzAction</code>) is frozen in   <code>agentkit_core.auth</code> (0.2.0+, no <code>PROTOCOL_VERSION</code> bump - it is above the waists).</li> <li>Real adapter (Superset) - M3. Multi-hop (<code>MAX_HOPS</code>) - M4. Skills - M5. MCP - M6.</li> <li><code>BiSemanticModel.calculated_fields</code> is optional (default <code>None</code>) pending the \u00a714 Q1   resolution at M3 (Superset + Metabase + Looker comparison).</li> </ul> <p>Auth-token transport (formerly the \u00a75.1-vs-\u00a710 open gap) is resolved in 0.2.0: <code>SessionContext.auth_token</code> via <code>Authorization</code> header, <code>exclude=True</code>. The full auth/authz seam (how that token + identity + permissions are derived) is the user-implemented <code>Authenticator</code>/<code>Authorizer</code> above.</p>"},{"location":"contracts/#versioning","title":"Versioning","text":"<p>Semantic versioning on <code>PROTOCOL_VERSION</code>:</p> <ul> <li>Patch: clarifications, additive optional fields that don't break existing parsers.</li> <li>Minor: additive (new event, new optional field, new verb, new envelope <code>kind</code>).   Existing consumers must ignore unknown kinds/events (forward-compat).</li> <li>Major: breaking (removed/renamed event, changed payload shape of an existing   event, changed <code>ComponentAdapter</code> method signature, changed envelope <code>kind</code> values).</li> </ul> <p>Two narrow waists are the strong constraint: any change to them bumps the version and updates this file + <code>agentkit-architecture.md</code>. Profile packages (<code>agentkit-bi</code>, future <code>agentkit-dcc</code>) version independently of <code>PROTOCOL_VERSION</code>.</p>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>agentkit_core<ul> <li>auth</li> <li>helpers</li> <li>llm</li> <li>models</li> <li>protocols</li> <li>testing</li> <li>verbs</li> </ul> </li> </ul>"},{"location":"reference/agentkit_core/","title":"Index","text":""},{"location":"reference/agentkit_core/#agentkit_core","title":"agentkit_core","text":"<p>agentkit-core: domain-neutral protocol + framework core (the two narrow waists).</p> <p>Frozen contracts (M1):   * Narrow waist \u2461 - SSE protocol: the 6 copilot events + QueryRequest/Message/SessionContext.   * Narrow waist \u2460 - adapter contract base types: Component / ComponentSchema envelope /     ComponentAdapter Protocol.</p> <p>Core is BI-agnostic (agentkit-bi / agentkit-dcc carry domain semantics). See docs/contracts.md for the frozen surface and versioning policy.</p> <p>Design note: <code>Component.schema_</code> is the Python attribute (trailing underscore avoids shadowing <code>BaseModel.schema</code>); it serializes to the wire key <code>schema</code>.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AdapterCapabilities","title":"AdapterCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-adapter capability flags. Domain-neutral base; profiles extend.</p> <p>Core holds genuinely cross-domain flags; profiles add domain flags (BI: can_filter/can_drill/can_add_widget/supports_semantic_model). Capability negotiation (\u00a75.2) reads core flags in core; profile flags in the profile.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AllowAllAuthorizer","title":"AllowAllAuthorizer","text":"<p>Trivial <code>Authorizer</code> that permits every action. For local/dev/mock only.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AllowAllAuthorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Always allow.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Always allow.\"\"\"\n    return None\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.Artifact","title":"Artifact","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic artifact envelope. Core defines generic kinds; profiles extend.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AuthContext","title":"AuthContext","text":"<p>               Bases: <code>BaseModel</code></p> <p>Transport-agnostic bag of raw auth material extracted from the inbound request.</p> <p>The app/runtime layer populates this: the web profile fills <code>headers</code>/<code>query</code>/ <code>cookies</code>/<code>peer</code> from the FastAPI <code>Request</code>; a DCC host fills them from the studio session. <code>Authenticator</code> reads whatever it needs (e.g. <code>context.header(\"Authorization\")</code>) and never sees an HTTP type, so the same implementation works across transports. <code>extra=\"allow\"</code> lets a runtime stash transport-specific extras without a subclass.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AuthContext.header","title":"header","text":"<pre><code>header(name: str) -&gt; str | None\n</code></pre> <p>Case-insensitive header lookup (HTTP headers are case-insensitive).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>def header(self, name: str) -&gt; str | None:\n    \"\"\"Case-insensitive header lookup (HTTP headers are case-insensitive).\"\"\"\n    lowered = name.lower()\n    for key, value in self.headers.items():\n        if key.lower() == lowered:\n            return value\n    return None\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.AuthError","title":"AuthError","text":"<p>               Bases: <code>Exception</code></p> <p>Base for authentication/authorization failures. Maps to HTTP 401/403.</p>"},{"location":"reference/agentkit_core/#agentkit_core.AuthenticationError","title":"AuthenticationError","text":"<p>               Bases: <code>AuthError</code></p> <p>Caller identity could not be verified (bad/expired/missing credential). HTTP 401.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Authenticator","title":"Authenticator","text":"<p>               Bases: <code>Protocol</code></p> <p>Verifies the caller's identity and produces a <code>Principal</code>.</p> <p>Implement JWT / OAuth2 / API-key / mTLS / SSO here. Read credentials from <code>context</code> (<code>context.header(\"Authorization\")</code>, cookies, peer cert, ...). Raise <code>AuthenticationError</code> on any failure; the app layer maps it to HTTP 401.</p> <p>Async so implementations may do remote work (token introspection, JWKS fetch, policy-server call); a purely local impl (signed-JWT verify) simply returns without awaiting.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Authenticator.authenticate","title":"authenticate  <code>async</code>","text":"<pre><code>authenticate(context: AuthContext) -&gt; Principal\n</code></pre> <p>Verify the caller and return their Principal (raise AuthenticationError on failure).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authenticate(self, context: AuthContext) -&gt; Principal:\n    \"\"\"Verify the caller and return their Principal (raise AuthenticationError on failure).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.AuthorizationError","title":"AuthorizationError","text":"<p>               Bases: <code>AuthError</code></p> <p>Caller is authenticated but lacks permission for the action. HTTP 403.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Authorizer","title":"Authorizer","text":"<p>               Bases: <code>Protocol</code></p> <p>Decides whether a <code>Principal</code> may perform an <code>AuthzAction</code>.</p> <p>Implement RLS / scope / ABAC checks here. Return normally to allow; raise <code>AuthorizationError</code> to deny (the app layer maps it to HTTP 403). Called per verb, before execution, with the verb name, target component, and args -- enough for field/row-level decisions.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Authorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Allow (return) or deny (raise AuthorizationError) the action for the principal.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Allow (return) or deny (raise AuthorizationError) the action for the principal.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.AuthzAction","title":"AuthzAction","text":"<p>               Bases: <code>BaseModel</code></p> <p>What is being authorized: a verb on an optional target component with its args.</p> <p>Passed to <code>Authorizer.authorize</code> before the verb executes. <code>component_id</code> is <code>None</code> for non-targeted verbs (<code>get_catalog</code>, <code>get_semantic_model</code>). <code>args</code> enables field/row-level checks (e.g. \"is this filter value allowed for this user?\").</p>"},{"location":"reference/agentkit_core/#agentkit_core.BaseSSE","title":"BaseSSE","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base for the 6 copilot SSE events.</p> <p>Wire contract (consumed by FastAPI <code>EventSourceResponse</code>)::</p> <pre><code>{\"event\": &lt;event name&gt;, \"data\": &lt;json string of payload&gt;}\n</code></pre> <p><code>event</code> is a class-level discriminator, not a model field, so it is not duplicated inside <code>data</code>.</p>"},{"location":"reference/agentkit_core/#agentkit_core.BaseSSE.to_sse","title":"to_sse","text":"<pre><code>to_sse() -&gt; dict[str, str]\n</code></pre> <p>Return the <code>{\"event\", \"data\"}</code> dict for SSE transport.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\models.py</code> <pre><code>def to_sse(self) -&gt; dict[str, str]:\n    \"\"\"Return the ``{\"event\", \"data\"}`` dict for SSE transport.\"\"\"\n    return {\n        \"event\": self.event,\n        \"data\": self.model_dump_json(by_alias=True, exclude_none=True),\n    }\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.Citation","title":"Citation","text":"<p>               Bases: <code>BaseModel</code></p> <p>A citation backing part of the answer.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Component","title":"Component","text":"<p>               Bases: <code>BaseModel</code></p> <p>A domain-neutral component (a BI widget, a DCC scene node, ...).</p> <p><code>schema_</code> carries the profile-typed semantic description; core treats it as an opaque envelope. The Python attribute is <code>schema_</code> (trailing underscore avoids shadowing <code>BaseModel.schema</code>); it serializes to the wire key <code>schema</code>.</p>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter","title":"ComponentAdapter","text":"<p>               Bases: <code>Protocol</code></p> <p>Narrow waist \u2460: translates a host tool's native objects into agentkit Components.</p> <p>Implementations live in adapter packages (agentkit-adapter-superset, ...). A runtime discovers adapters via the <code>agentkit.adapters</code> entry-point group and never statically depends on them (\u00a7C.1). <code>origin</code> + <code>capabilities</code> are instance attributes; the methods map 1:1 to the backend-sync verbs (\u00a7D.4).</p>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter.get_component","title":"get_component  <code>async</code>","text":"<pre><code>get_component(ctx: SessionContext, component_id: str) -&gt; Component\n</code></pre> <p>Return a single component by id.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_component(self, ctx: SessionContext, component_id: str) -&gt; Component:\n    \"\"\"Return a single component by id.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter.get_component_data","title":"get_component_data  <code>async</code>","text":"<pre><code>get_component_data(ctx: SessionContext, component: Component, input_args: dict[str, Any]) -&gt; ComponentData\n</code></pre> <p>Fetch data for a component (verb: get_component_data). RLS via ctx.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_component_data(\n    self, ctx: SessionContext, component: Component, input_args: dict[str, Any]\n) -&gt; ComponentData:\n    \"\"\"Fetch data for a component (verb: get_component_data). RLS via ctx.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter.get_semantic_model","title":"get_semantic_model  <code>async</code>","text":"<pre><code>get_semantic_model(ctx: SessionContext, component: Component) -&gt; ComponentSchema\n</code></pre> <p>Fetch the semantic model (schema) of a component (verb: get_semantic_model).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_semantic_model(self, ctx: SessionContext, component: Component) -&gt; ComponentSchema:\n    \"\"\"Fetch the semantic model (schema) of a component (verb: get_semantic_model).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter.list_components","title":"list_components  <code>async</code>","text":"<pre><code>list_components(ctx: SessionContext) -&gt; list[Component]\n</code></pre> <p>Return the catalog of available components (verb: get_catalog).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def list_components(self, ctx: SessionContext) -&gt; list[Component]:\n    \"\"\"Return the catalog of available components (verb: get_catalog).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentAdapter.refine_component","title":"refine_component  <code>async</code>","text":"<pre><code>refine_component(ctx: SessionContext, component: Component, refinement: Refinement) -&gt; ComponentData\n</code></pre> <p>Refine an already-fetched component via its semantic model (verb: refine_component).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def refine_component(\n    self, ctx: SessionContext, component: Component, refinement: Refinement\n) -&gt; ComponentData:\n    \"\"\"Refine an already-fetched component via its semantic model (verb: refine_component).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentCapabilities","title":"ComponentCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-component capability flags. Domain-neutral base; profiles extend.</p> <p>agentkit-bi defines <code>WidgetCapabilities</code> adding can_filter/can_drill/can_export. Core does not read profile-specific flags.</p>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentData","title":"ComponentData","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic data envelope returned by <code>get_component_data</code>.</p> <p>Profiles subclass with typed payloads (BI: columns/rows; DCC: scene graph).</p>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentParam","title":"ComponentParam","text":"<p>               Bases: <code>BaseModel</code></p> <p>A parameter a component accepts (filter value, time range, ...).</p>"},{"location":"reference/agentkit_core/#agentkit_core.ComponentSchema","title":"ComponentSchema","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic schema envelope. Core does NOT parse content.</p> <p>Profiles subclass with a <code>kind</code> Literal + typed fields, e.g. agentkit-bi's <code>BiSemanticModel</code> (kind=\"bi.semantic\"). Adapters return profile-typed instances; core serializes them transparently (subclass fields preserved via SerializeAsAny) and deserializes them back as this opaque base (profile fields kept via <code>extra=\"allow\"</code>).</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotCitationCollection","title":"CopilotCitationCollection","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Citations backing the answer.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotFunctionCall","title":"CopilotFunctionCall","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Ask the frontend to execute a UI verb (Option B: frontend FunctionCall only).</p> <p>Carries the verb name + arguments; the frontend executes, then re-POSTs with a <code>role=tool</code> message containing the result. Reserved for UI actions only (add/update/manage_nav/assign_tasks); data/skill/MCP go via backend sync calls.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotMessageArtifact","title":"CopilotMessageArtifact","text":"<p>               Bases: <code>BaseSSE</code></p> <p>A structured artifact (chart/table/text/...) to render in the UI.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotMessageChunk","title":"CopilotMessageChunk","text":"<p>               Bases: <code>BaseSSE</code></p> <p>A streamed text delta of the assistant's reply.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotPromptSuggestions","title":"CopilotPromptSuggestions","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Follow-up prompt suggestions.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse","title":"CopilotResponse","text":"<pre><code>CopilotResponse(events: list[BaseSSE])\n</code></pre> <p>Collected SSE stream with OpenBB-style assertion helpers.</p> <p>Store the raw event list (copied).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def __init__(self, events: list[BaseSSE]) -&gt; None:\n    \"\"\"Store the raw event list (copied).\"\"\"\n    self.events: list[BaseSSE] = list(events)\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.artifacts","title":"artifacts  <code>property</code>","text":"<pre><code>artifacts: list[Artifact]\n</code></pre> <p>All artifacts carried by copilotMessageArtifact events.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.citations","title":"citations  <code>property</code>","text":"<pre><code>citations: list[Citation]\n</code></pre> <p>All citations across copilotCitationCollection events.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.function_calls","title":"function_calls  <code>property</code>","text":"<pre><code>function_calls: list[CopilotFunctionCall]\n</code></pre> <p>All copilotFunctionCall events (frontend UI actions requested).</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.suggestions","title":"suggestions  <code>property</code>","text":"<pre><code>suggestions: list[str]\n</code></pre> <p>All follow-up suggestions across copilotPromptSuggestions events.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.text","title":"text  <code>property</code>","text":"<pre><code>text: str\n</code></pre> <p>Concatenated text of all copilotMessageChunk events.</p>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Concise debug representation.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Concise debug representation.\"\"\"\n    return (\n        f\"CopilotResponse(events={len(self.events)}, \"\n        f\"text={self.text!r}, artifacts={len(self.artifacts)}, \"\n        f\"function_calls={len(self.function_calls)})\"\n    )\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.has_artifact","title":"has_artifact","text":"<pre><code>has_artifact(kind: str | None = None) -&gt; bool\n</code></pre> <p>True if any artifact was emitted; if <code>kind</code> given, match that kind.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_artifact(self, kind: str | None = None) -&gt; bool:\n    \"\"\"True if any artifact was emitted; if ``kind`` given, match that kind.\"\"\"\n    if kind is None:\n        return len(self.artifacts) &gt; 0\n    return any(a.kind == kind for a in self.artifacts)\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.has_function_call","title":"has_function_call","text":"<pre><code>has_function_call(name: str | None = None) -&gt; bool\n</code></pre> <p>True if any function call was emitted; if <code>name</code> given, match that verb.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_function_call(self, name: str | None = None) -&gt; bool:\n    \"\"\"True if any function call was emitted; if ``name`` given, match that verb.\"\"\"\n    if name is None:\n        return len(self.function_calls) &gt; 0\n    return any(fc.name == name for fc in self.function_calls)\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotResponse.has_text_containing","title":"has_text_containing","text":"<pre><code>has_text_containing(needle: str) -&gt; bool\n</code></pre> <p>True if the concatenated reply text contains <code>needle</code>.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_text_containing(self, needle: str) -&gt; bool:\n    \"\"\"True if the concatenated reply text contains ``needle``.\"\"\"\n    return needle in self.text\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.CopilotStatusUpdate","title":"CopilotStatusUpdate","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Lifecycle/status signal (thinking, fetching data, idle ...).</p>"},{"location":"reference/agentkit_core/#agentkit_core.DenyAllAuthorizer","title":"DenyAllAuthorizer","text":"<p>Trivial <code>Authorizer</code> that denies every action. For testing denial paths.</p>"},{"location":"reference/agentkit_core/#agentkit_core.DenyAllAuthorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Always deny.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Always deny.\"\"\"\n    raise AuthorizationError(\n        f\"denied: verb={action.verb} component={action.component_id} user={principal.user_identity}\"\n    )\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.ErrorArtifact","title":"ErrorArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>An error artifact (e.g. a failed tool result).</p>"},{"location":"reference/agentkit_core/#agentkit_core.Field","title":"Field","text":"<p>               Bases: <code>BaseModel</code></p> <p>A generic named field. Shared by BI (dimensions/measures) and DCC (attributes).</p>"},{"location":"reference/agentkit_core/#agentkit_core.LlmChunk","title":"LlmChunk","text":"<p>               Bases: <code>BaseModel</code></p> <p>A streamed chunk from the LLM (text delta).</p>"},{"location":"reference/agentkit_core/#agentkit_core.LlmClient","title":"LlmClient","text":"<p>               Bases: <code>Protocol</code></p> <p>LLM call layer. Providers swap via env; agent code is provider-agnostic (\u00a76.4).</p> Two methods map to the Option B two-phase loop <ul> <li><code>call</code> (Phase 1, non-streaming, with tools): returns a <code>LlmResponse</code> whose   <code>tool_calls</code> the orchestrator routes (backend sync vs frontend FunctionCall).</li> <li><code>stream</code> (Phase 2, streaming, usually without tools): yields <code>LlmChunk</code>   text deltas for the final answer.</li> </ul> <p>The M2 default impl (agentkit-llm-langchain) wraps a langchain ChatModel and converts between these provider-neutral types and langchain's own.</p>"},{"location":"reference/agentkit_core/#agentkit_core.LlmClient.call","title":"call  <code>async</code>","text":"<pre><code>call(messages: list[Any], tools: list[ToolDef] | None = None) -&gt; LlmResponse\n</code></pre> <p>Return a full response (Phase 1: with tools to obtain tool_calls).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def call(\n    self,\n    messages: list[Any],\n    tools: list[ToolDef] | None = None,\n) -&gt; LlmResponse:\n    \"\"\"Return a full response (Phase 1: with tools to obtain tool_calls).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.LlmClient.stream","title":"stream","text":"<pre><code>stream(messages: list[Any], tools: list[ToolDef] | None = None) -&gt; AsyncIterator[LlmChunk]\n</code></pre> <p>Yield streamed text chunks (async generator). Phase 2 final answer.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>def stream(\n    self,\n    messages: list[Any],\n    tools: list[ToolDef] | None = None,\n) -&gt; AsyncIterator[LlmChunk]:\n    \"\"\"Yield streamed text chunks (async generator). Phase 2 final answer.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.LlmResponse","title":"LlmResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>A full (non-streaming) LLM response: text content and/or tool calls.</p> <p>Phase 1 of the Option B loop calls <code>LlmClient.call</code> with tools to obtain this. If <code>tool_calls</code> is non-empty the orchestrator routes them (backend sync, or frontend FunctionCall); otherwise <code>content</code> is the answer (yielded as chunks).</p>"},{"location":"reference/agentkit_core/#agentkit_core.LlmToolCall","title":"LlmToolCall","text":"<p>               Bases: <code>BaseModel</code></p> <p>A tool call requested by the LLM (provider-neutral).</p>"},{"location":"reference/agentkit_core/#agentkit_core.MarkdownArtifact","title":"MarkdownArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A Markdown-formatted artifact.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Message","title":"Message","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single conversation message. <code>role</code> drives the state machine (\u00a76.3).</p> <ul> <li>role=human: run the agent (sync multi-hop fetch + stream).</li> <li>role=tool: a frontend UI action result returned via the FunctionCall loop.</li> <li>role=assistant/system: history context.</li> </ul>"},{"location":"reference/agentkit_core/#agentkit_core.Orchestrator","title":"Orchestrator","text":"<p>               Bases: <code>Protocol</code></p> <p>Runs a query and yields the SSE stream. Stateless: state lives in request.messages.</p> <p><code>run</code> is an async generator: calling it returns an <code>AsyncIterator[BaseSSE]</code> (not a coroutine). The M2 impl (StatelessOrchestrator) follows the Option B core loop (\u00a76.1): backend-sync fetch within the request, streaming chunks/artifacts.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Orchestrator.run","title":"run","text":"<pre><code>run(request: QueryRequest) -&gt; AsyncIterator[BaseSSE]\n</code></pre> <p>Yield the SSE stream for a query (async generator, not a coroutine).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>def run(self, request: QueryRequest) -&gt; AsyncIterator[BaseSSE]:\n    \"\"\"Yield the SSE stream for a query (async generator, not a coroutine).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.Principal","title":"Principal","text":"<p>               Bases: <code>BaseModel</code></p> <p>The authenticated subject produced by <code>Authenticator.authenticate</code>.</p> <p>Carries who the user is (<code>user_identity</code>), what they may do (<code>user_permissions</code>), and a delegated credential for backend data calls (<code>auth_token</code>, Option B). The app layer bridges it onto a <code>SessionContext</code> via <code>session_from_principal</code>. <code>auth_token</code> is the delegated BI token (<code>SecretStr</code>, never serialized) -- adapters fetch data under this identity, never a service account (\u00a710). Frozen: a verified identity must not be mutated mid-request.</p>"},{"location":"reference/agentkit_core/#agentkit_core.QueryRequest","title":"QueryRequest","text":"<p>               Bases: <code>BaseModel</code></p> <p>Inbound request. Stateless: all state lives in <code>messages</code>.</p>"},{"location":"reference/agentkit_core/#agentkit_core.Refinement","title":"Refinement","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic refinement envelope for <code>refine_component</code>.</p> <p>Profiles subclass (BI: filters/drill/measure swap). Core treats as opaque.</p>"},{"location":"reference/agentkit_core/#agentkit_core.SessionContext","title":"SessionContext","text":"<p>               Bases: <code>BaseModel</code></p> <p>Carries user identity + permissions + BI auth token; the RLS passthrough vehicle.</p> <p><code>auth_token</code> is the user's BI token for Option B backend-fetch (the orchestrator calls the adapter on the user's behalf). Transported via HTTP <code>Authorization</code> header (extracted by the FastAPI layer), NOT the request body. It is <code>SecretStr</code> with <code>exclude=True</code> so it is never serialized into <code>model_dump()</code> / JSON / logs (\u00a710: short-lived, not logged/cached, transport-encrypted, scoped to the workspace). Adapters read it via <code>ctx.auth_token.get_secret_value()</code>.</p>"},{"location":"reference/agentkit_core/#agentkit_core.TableArtifact","title":"TableArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A tabular artifact (columns + rows).</p>"},{"location":"reference/agentkit_core/#agentkit_core.TextArtifact","title":"TextArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A plain-text artifact.</p>"},{"location":"reference/agentkit_core/#agentkit_core.ToolDef","title":"ToolDef","text":"<p>               Bases: <code>BaseModel</code></p> <p>A provider-neutral tool definition exposed to the LLM.</p> <p>Built from a <code>VerbSpec</code> (name/description/input_schema). The <code>executes_on</code> category is deliberately NOT included - the LLM must not see it; the orchestrator routes by it after the LLM emits a tool call.</p>"},{"location":"reference/agentkit_core/#agentkit_core.VerbSpec","title":"VerbSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>Declarative verb definition: name, category, and JSON-schema input.</p>"},{"location":"reference/agentkit_core/#agentkit_core.collect_stream","title":"collect_stream  <code>async</code>","text":"<pre><code>collect_stream(stream: AsyncIterator[BaseSSE]) -&gt; CopilotResponse\n</code></pre> <p>Drain an SSE async iterator into a CopilotResponse.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>async def collect_stream(stream: AsyncIterator[BaseSSE]) -&gt; CopilotResponse:\n    \"\"\"Drain an SSE async iterator into a CopilotResponse.\"\"\"\n    events: list[BaseSSE] = []\n    async for event in stream:\n        events.append(event)\n    return CopilotResponse(events)\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.deterministic_uuid","title":"deterministic_uuid","text":"<pre><code>deterministic_uuid(origin: str, component_id: str) -&gt; str\n</code></pre> <p>Stable cross-turn component UUID: <code>xxhash(origin + component_id)</code>.</p> <p>Determinism lets the frontend reconcile the same component across FunctionCall loop round-trips (\u00a74.1). Same inputs -&gt; same UUID, forever, across processes.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\helpers.py</code> <pre><code>def deterministic_uuid(origin: str, component_id: str) -&gt; str:\n    \"\"\"Stable cross-turn component UUID: ``xxhash(origin + component_id)``.\n\n    Determinism lets the frontend reconcile the same component across FunctionCall\n    loop round-trips (\u00a74.1). Same inputs -&gt; same UUID, forever, across processes.\n    \"\"\"\n    h = xxhash.xxh64()\n    h.update(f\"{origin}:{component_id}\".encode())\n    return h.hexdigest()\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.human_message","title":"human_message","text":"<pre><code>human_message(text: str) -&gt; Message\n</code></pre> <p>Build a role=human message.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def human_message(text: str) -&gt; Message:\n    \"\"\"Build a role=human message.\"\"\"\n    return Message(role=\"human\", content=text)\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.query","title":"query","text":"<pre><code>query(text: str, *, user_identity: str = 'tester', workspace_id: str = 'ws-test', trace_id: str = 'trace-test', user_permissions: list[str] | None = None, history: list[Message] | None = None, **kwargs: Any) -&gt; QueryRequest\n</code></pre> <p>Build a minimal QueryRequest for tests.</p> <p>Extra keyword args forward to QueryRequest (tools, features, workspace_options, ...).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def query(\n    text: str,\n    *,\n    user_identity: str = \"tester\",\n    workspace_id: str = \"ws-test\",\n    trace_id: str = \"trace-test\",\n    user_permissions: list[str] | None = None,\n    history: list[Message] | None = None,\n    **kwargs: Any,\n) -&gt; QueryRequest:\n    \"\"\"Build a minimal QueryRequest for tests.\n\n    Extra keyword args forward to QueryRequest (tools, features, workspace_options, ...).\n    \"\"\"\n    messages = list(history or [])\n    messages.append(human_message(text))\n    return QueryRequest(\n        messages=messages,\n        session_context=SessionContext(\n            user_identity=user_identity,\n            user_permissions=user_permissions or [],\n            workspace_id=workspace_id,\n            trace_id=trace_id,\n        ),\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/agentkit_core/#agentkit_core.session_from_principal","title":"session_from_principal","text":"<pre><code>session_from_principal(principal: Principal, *, trace_id: str, workspace_id: str | None = None) -&gt; SessionContext\n</code></pre> <p>Build a <code>SessionContext</code> from an authenticated <code>Principal</code>.</p> <p>The app layer calls this after <code>Authenticator.authenticate</code> succeeds. <code>trace_id</code> is always a runtime concern; <code>workspace_id</code> may come from the Principal (e.g. a JWT claim) or be supplied by the runtime (e.g. a path parameter) -- the explicit argument wins. The Principal's <code>auth_token</code>, <code>user_identity</code> and <code>user_permissions</code> flow straight through onto the wire <code>SessionContext</code>.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>def session_from_principal(\n    principal: Principal,\n    *,\n    trace_id: str,\n    workspace_id: str | None = None,\n) -&gt; SessionContext:\n    \"\"\"Build a ``SessionContext`` from an authenticated ``Principal``.\n\n    The app layer calls this after ``Authenticator.authenticate`` succeeds. ``trace_id``\n    is always a runtime concern; ``workspace_id`` may come from the Principal (e.g. a JWT\n    claim) or be supplied by the runtime (e.g. a path parameter) -- the explicit argument\n    wins. The Principal's ``auth_token``, ``user_identity`` and ``user_permissions`` flow\n    straight through onto the wire ``SessionContext``.\n    \"\"\"\n    resolved_workspace = workspace_id or principal.workspace_id\n    if resolved_workspace is None:\n        raise ValueError(\"workspace_id is required: pass it explicitly or set Principal.workspace_id\")\n    return SessionContext(\n        user_identity=principal.user_identity,\n        user_permissions=list(principal.user_permissions),\n        workspace_id=resolved_workspace,\n        trace_id=trace_id,\n        auth_token=principal.auth_token,\n    )\n</code></pre>"},{"location":"reference/agentkit_core/auth/","title":"auth","text":""},{"location":"reference/agentkit_core/auth/#agentkit_core.auth","title":"agentkit_core.auth","text":"<p>Authentication &amp; authorization extension points (the auth seam).</p> <p>Auth is a cross-cutting framework contract, NOT part of either narrow waist: it runs before a request enters the SSE/orchestrator path, so adding it does not bump <code>PROTOCOL_VERSION</code>. The contracts here are transport-agnostic so one implementation serves both the web profile (FastAPI, HTTP headers) and the DCC profile (PySide/Maya host session) -- <code>Authenticator</code> never sees an HTTP request type.</p> <p>Users plug in their own mechanism (JWT, OAuth2, API-key, mTLS, studio SSO, ...) by implementing the two Protocols. The framework ships only:</p> <ul> <li>the contracts (<code>AuthContext</code> / <code>Principal</code> / <code>AuthzAction</code>),</li> <li>the two Protocols (<code>Authenticator</code> / <code>Authorizer</code>),</li> <li>trivial dev/test authorizers (<code>AllowAllAuthorizer</code> / <code>DenyAllAuthorizer</code>),</li> <li>a bridge helper <code>session_from_principal</code> mapping a verified <code>Principal</code> onto the     wire <code>SessionContext</code> the orchestrator consumes.</li> </ul> <p>Security invariants (\u00a710) are enforced by the implementations, not here: adapters fetch data under the Principal's identity (never a service account), the delegated <code>auth_token</code> is short-lived / unlogged / uncached, and row-level scoping happens in <code>Authorizer</code> + adapter RLS.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AllowAllAuthorizer","title":"AllowAllAuthorizer","text":"<p>Trivial <code>Authorizer</code> that permits every action. For local/dev/mock only.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AllowAllAuthorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Always allow.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Always allow.\"\"\"\n    return None\n</code></pre>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthContext","title":"AuthContext","text":"<p>               Bases: <code>BaseModel</code></p> <p>Transport-agnostic bag of raw auth material extracted from the inbound request.</p> <p>The app/runtime layer populates this: the web profile fills <code>headers</code>/<code>query</code>/ <code>cookies</code>/<code>peer</code> from the FastAPI <code>Request</code>; a DCC host fills them from the studio session. <code>Authenticator</code> reads whatever it needs (e.g. <code>context.header(\"Authorization\")</code>) and never sees an HTTP type, so the same implementation works across transports. <code>extra=\"allow\"</code> lets a runtime stash transport-specific extras without a subclass.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthContext.header","title":"header","text":"<pre><code>header(name: str) -&gt; str | None\n</code></pre> <p>Case-insensitive header lookup (HTTP headers are case-insensitive).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>def header(self, name: str) -&gt; str | None:\n    \"\"\"Case-insensitive header lookup (HTTP headers are case-insensitive).\"\"\"\n    lowered = name.lower()\n    for key, value in self.headers.items():\n        if key.lower() == lowered:\n            return value\n    return None\n</code></pre>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthError","title":"AuthError","text":"<p>               Bases: <code>Exception</code></p> <p>Base for authentication/authorization failures. Maps to HTTP 401/403.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthenticationError","title":"AuthenticationError","text":"<p>               Bases: <code>AuthError</code></p> <p>Caller identity could not be verified (bad/expired/missing credential). HTTP 401.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.Authenticator","title":"Authenticator","text":"<p>               Bases: <code>Protocol</code></p> <p>Verifies the caller's identity and produces a <code>Principal</code>.</p> <p>Implement JWT / OAuth2 / API-key / mTLS / SSO here. Read credentials from <code>context</code> (<code>context.header(\"Authorization\")</code>, cookies, peer cert, ...). Raise <code>AuthenticationError</code> on any failure; the app layer maps it to HTTP 401.</p> <p>Async so implementations may do remote work (token introspection, JWKS fetch, policy-server call); a purely local impl (signed-JWT verify) simply returns without awaiting.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.Authenticator.authenticate","title":"authenticate  <code>async</code>","text":"<pre><code>authenticate(context: AuthContext) -&gt; Principal\n</code></pre> <p>Verify the caller and return their Principal (raise AuthenticationError on failure).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authenticate(self, context: AuthContext) -&gt; Principal:\n    \"\"\"Verify the caller and return their Principal (raise AuthenticationError on failure).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthorizationError","title":"AuthorizationError","text":"<p>               Bases: <code>AuthError</code></p> <p>Caller is authenticated but lacks permission for the action. HTTP 403.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.Authorizer","title":"Authorizer","text":"<p>               Bases: <code>Protocol</code></p> <p>Decides whether a <code>Principal</code> may perform an <code>AuthzAction</code>.</p> <p>Implement RLS / scope / ABAC checks here. Return normally to allow; raise <code>AuthorizationError</code> to deny (the app layer maps it to HTTP 403). Called per verb, before execution, with the verb name, target component, and args -- enough for field/row-level decisions.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.Authorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Allow (return) or deny (raise AuthorizationError) the action for the principal.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Allow (return) or deny (raise AuthorizationError) the action for the principal.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.AuthzAction","title":"AuthzAction","text":"<p>               Bases: <code>BaseModel</code></p> <p>What is being authorized: a verb on an optional target component with its args.</p> <p>Passed to <code>Authorizer.authorize</code> before the verb executes. <code>component_id</code> is <code>None</code> for non-targeted verbs (<code>get_catalog</code>, <code>get_semantic_model</code>). <code>args</code> enables field/row-level checks (e.g. \"is this filter value allowed for this user?\").</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.DenyAllAuthorizer","title":"DenyAllAuthorizer","text":"<p>Trivial <code>Authorizer</code> that denies every action. For testing denial paths.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.DenyAllAuthorizer.authorize","title":"authorize  <code>async</code>","text":"<pre><code>authorize(principal: Principal, action: AuthzAction) -&gt; None\n</code></pre> <p>Always deny.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>async def authorize(self, principal: Principal, action: AuthzAction) -&gt; None:\n    \"\"\"Always deny.\"\"\"\n    raise AuthorizationError(\n        f\"denied: verb={action.verb} component={action.component_id} user={principal.user_identity}\"\n    )\n</code></pre>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.Principal","title":"Principal","text":"<p>               Bases: <code>BaseModel</code></p> <p>The authenticated subject produced by <code>Authenticator.authenticate</code>.</p> <p>Carries who the user is (<code>user_identity</code>), what they may do (<code>user_permissions</code>), and a delegated credential for backend data calls (<code>auth_token</code>, Option B). The app layer bridges it onto a <code>SessionContext</code> via <code>session_from_principal</code>. <code>auth_token</code> is the delegated BI token (<code>SecretStr</code>, never serialized) -- adapters fetch data under this identity, never a service account (\u00a710). Frozen: a verified identity must not be mutated mid-request.</p>"},{"location":"reference/agentkit_core/auth/#agentkit_core.auth.session_from_principal","title":"session_from_principal","text":"<pre><code>session_from_principal(principal: Principal, *, trace_id: str, workspace_id: str | None = None) -&gt; SessionContext\n</code></pre> <p>Build a <code>SessionContext</code> from an authenticated <code>Principal</code>.</p> <p>The app layer calls this after <code>Authenticator.authenticate</code> succeeds. <code>trace_id</code> is always a runtime concern; <code>workspace_id</code> may come from the Principal (e.g. a JWT claim) or be supplied by the runtime (e.g. a path parameter) -- the explicit argument wins. The Principal's <code>auth_token</code>, <code>user_identity</code> and <code>user_permissions</code> flow straight through onto the wire <code>SessionContext</code>.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\auth.py</code> <pre><code>def session_from_principal(\n    principal: Principal,\n    *,\n    trace_id: str,\n    workspace_id: str | None = None,\n) -&gt; SessionContext:\n    \"\"\"Build a ``SessionContext`` from an authenticated ``Principal``.\n\n    The app layer calls this after ``Authenticator.authenticate`` succeeds. ``trace_id``\n    is always a runtime concern; ``workspace_id`` may come from the Principal (e.g. a JWT\n    claim) or be supplied by the runtime (e.g. a path parameter) -- the explicit argument\n    wins. The Principal's ``auth_token``, ``user_identity`` and ``user_permissions`` flow\n    straight through onto the wire ``SessionContext``.\n    \"\"\"\n    resolved_workspace = workspace_id or principal.workspace_id\n    if resolved_workspace is None:\n        raise ValueError(\"workspace_id is required: pass it explicitly or set Principal.workspace_id\")\n    return SessionContext(\n        user_identity=principal.user_identity,\n        user_permissions=list(principal.user_permissions),\n        workspace_id=resolved_workspace,\n        trace_id=trace_id,\n        auth_token=principal.auth_token,\n    )\n</code></pre>"},{"location":"reference/agentkit_core/helpers/","title":"helpers","text":""},{"location":"reference/agentkit_core/helpers/#agentkit_core.helpers","title":"agentkit_core.helpers","text":"<p>Protocol helpers: deterministic IDs and SSE serialization utilities.</p>"},{"location":"reference/agentkit_core/helpers/#agentkit_core.helpers.deterministic_uuid","title":"deterministic_uuid","text":"<pre><code>deterministic_uuid(origin: str, component_id: str) -&gt; str\n</code></pre> <p>Stable cross-turn component UUID: <code>xxhash(origin + component_id)</code>.</p> <p>Determinism lets the frontend reconcile the same component across FunctionCall loop round-trips (\u00a74.1). Same inputs -&gt; same UUID, forever, across processes.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\helpers.py</code> <pre><code>def deterministic_uuid(origin: str, component_id: str) -&gt; str:\n    \"\"\"Stable cross-turn component UUID: ``xxhash(origin + component_id)``.\n\n    Determinism lets the frontend reconcile the same component across FunctionCall\n    loop round-trips (\u00a74.1). Same inputs -&gt; same UUID, forever, across processes.\n    \"\"\"\n    h = xxhash.xxh64()\n    h.update(f\"{origin}:{component_id}\".encode())\n    return h.hexdigest()\n</code></pre>"},{"location":"reference/agentkit_core/llm/","title":"llm","text":""},{"location":"reference/agentkit_core/llm/#agentkit_core.llm","title":"agentkit_core.llm","text":"<p>LLM result + tool-definition types (provider-neutral).</p> <p>These let the orchestrator route LLM tool calls (backend sync vs frontend FunctionCall) without depending on any LLM provider SDK. The <code>LlmClient</code> impl (agentkit-llm-langchain) converts between these and langchain's own types.</p> <p>Added in 0.2.0 (PROTOCOL_VERSION minor bump): <code>LlmClient</code> is tightened to return these instead of <code>Any</code>.</p>"},{"location":"reference/agentkit_core/llm/#agentkit_core.llm.LlmChunk","title":"LlmChunk","text":"<p>               Bases: <code>BaseModel</code></p> <p>A streamed chunk from the LLM (text delta).</p>"},{"location":"reference/agentkit_core/llm/#agentkit_core.llm.LlmResponse","title":"LlmResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>A full (non-streaming) LLM response: text content and/or tool calls.</p> <p>Phase 1 of the Option B loop calls <code>LlmClient.call</code> with tools to obtain this. If <code>tool_calls</code> is non-empty the orchestrator routes them (backend sync, or frontend FunctionCall); otherwise <code>content</code> is the answer (yielded as chunks).</p>"},{"location":"reference/agentkit_core/llm/#agentkit_core.llm.LlmToolCall","title":"LlmToolCall","text":"<p>               Bases: <code>BaseModel</code></p> <p>A tool call requested by the LLM (provider-neutral).</p>"},{"location":"reference/agentkit_core/llm/#agentkit_core.llm.ToolDef","title":"ToolDef","text":"<p>               Bases: <code>BaseModel</code></p> <p>A provider-neutral tool definition exposed to the LLM.</p> <p>Built from a <code>VerbSpec</code> (name/description/input_schema). The <code>executes_on</code> category is deliberately NOT included - the LLM must not see it; the orchestrator routes by it after the LLM emits a tool call.</p>"},{"location":"reference/agentkit_core/models/","title":"models","text":""},{"location":"reference/agentkit_core/models/#agentkit_core.models","title":"agentkit_core.models","text":"<p>agentkit-core protocol models: the two narrow waists, frozen.</p> Freezes <ul> <li>Narrow waist \u2461 (SSE protocol) - the 6 copilot events + QueryRequest/Message/SessionContext.</li> <li>Narrow waist \u2460 (adapter contract) base types - Component / ComponentSchema envelope /   ComponentCapabilities / AdapterCapabilities. The ComponentAdapter Protocol itself   lives in :mod:<code>agentkit_core.protocols</code>.</li> </ul> <p>Core is domain-neutral. Domain schemas (BiSemanticModel, DccSchema) and artifacts (chart) live in profile packages (agentkit-bi, agentkit-dcc). Core NEVER parses the content of a ComponentSchema or Artifact: they are polymorphic envelopes (<code>kind</code> discriminator + <code>extra=\"allow\"</code>), and subclass-typed fields use <code>SerializeAsAny</code> so profile payloads survive JSON round-trip through core unchanged.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.AdapterCapabilities","title":"AdapterCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-adapter capability flags. Domain-neutral base; profiles extend.</p> <p>Core holds genuinely cross-domain flags; profiles add domain flags (BI: can_filter/can_drill/can_add_widget/supports_semantic_model). Capability negotiation (\u00a75.2) reads core flags in core; profile flags in the profile.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Artifact","title":"Artifact","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic artifact envelope. Core defines generic kinds; profiles extend.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.BaseSSE","title":"BaseSSE","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base for the 6 copilot SSE events.</p> <p>Wire contract (consumed by FastAPI <code>EventSourceResponse</code>)::</p> <pre><code>{\"event\": &lt;event name&gt;, \"data\": &lt;json string of payload&gt;}\n</code></pre> <p><code>event</code> is a class-level discriminator, not a model field, so it is not duplicated inside <code>data</code>.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.BaseSSE.to_sse","title":"to_sse","text":"<pre><code>to_sse() -&gt; dict[str, str]\n</code></pre> <p>Return the <code>{\"event\", \"data\"}</code> dict for SSE transport.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\models.py</code> <pre><code>def to_sse(self) -&gt; dict[str, str]:\n    \"\"\"Return the ``{\"event\", \"data\"}`` dict for SSE transport.\"\"\"\n    return {\n        \"event\": self.event,\n        \"data\": self.model_dump_json(by_alias=True, exclude_none=True),\n    }\n</code></pre>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Citation","title":"Citation","text":"<p>               Bases: <code>BaseModel</code></p> <p>A citation backing part of the answer.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Component","title":"Component","text":"<p>               Bases: <code>BaseModel</code></p> <p>A domain-neutral component (a BI widget, a DCC scene node, ...).</p> <p><code>schema_</code> carries the profile-typed semantic description; core treats it as an opaque envelope. The Python attribute is <code>schema_</code> (trailing underscore avoids shadowing <code>BaseModel.schema</code>); it serializes to the wire key <code>schema</code>.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.ComponentCapabilities","title":"ComponentCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Per-component capability flags. Domain-neutral base; profiles extend.</p> <p>agentkit-bi defines <code>WidgetCapabilities</code> adding can_filter/can_drill/can_export. Core does not read profile-specific flags.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.ComponentData","title":"ComponentData","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic data envelope returned by <code>get_component_data</code>.</p> <p>Profiles subclass with typed payloads (BI: columns/rows; DCC: scene graph).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.ComponentParam","title":"ComponentParam","text":"<p>               Bases: <code>BaseModel</code></p> <p>A parameter a component accepts (filter value, time range, ...).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.ComponentSchema","title":"ComponentSchema","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic schema envelope. Core does NOT parse content.</p> <p>Profiles subclass with a <code>kind</code> Literal + typed fields, e.g. agentkit-bi's <code>BiSemanticModel</code> (kind=\"bi.semantic\"). Adapters return profile-typed instances; core serializes them transparently (subclass fields preserved via SerializeAsAny) and deserializes them back as this opaque base (profile fields kept via <code>extra=\"allow\"</code>).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotCitationCollection","title":"CopilotCitationCollection","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Citations backing the answer.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotFunctionCall","title":"CopilotFunctionCall","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Ask the frontend to execute a UI verb (Option B: frontend FunctionCall only).</p> <p>Carries the verb name + arguments; the frontend executes, then re-POSTs with a <code>role=tool</code> message containing the result. Reserved for UI actions only (add/update/manage_nav/assign_tasks); data/skill/MCP go via backend sync calls.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotMessageArtifact","title":"CopilotMessageArtifact","text":"<p>               Bases: <code>BaseSSE</code></p> <p>A structured artifact (chart/table/text/...) to render in the UI.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotMessageChunk","title":"CopilotMessageChunk","text":"<p>               Bases: <code>BaseSSE</code></p> <p>A streamed text delta of the assistant's reply.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotPromptSuggestions","title":"CopilotPromptSuggestions","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Follow-up prompt suggestions.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.CopilotStatusUpdate","title":"CopilotStatusUpdate","text":"<p>               Bases: <code>BaseSSE</code></p> <p>Lifecycle/status signal (thinking, fetching data, idle ...).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.ErrorArtifact","title":"ErrorArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>An error artifact (e.g. a failed tool result).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Field","title":"Field","text":"<p>               Bases: <code>BaseModel</code></p> <p>A generic named field. Shared by BI (dimensions/measures) and DCC (attributes).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.MarkdownArtifact","title":"MarkdownArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A Markdown-formatted artifact.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Message","title":"Message","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single conversation message. <code>role</code> drives the state machine (\u00a76.3).</p> <ul> <li>role=human: run the agent (sync multi-hop fetch + stream).</li> <li>role=tool: a frontend UI action result returned via the FunctionCall loop.</li> <li>role=assistant/system: history context.</li> </ul>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.QueryRequest","title":"QueryRequest","text":"<p>               Bases: <code>BaseModel</code></p> <p>Inbound request. Stateless: all state lives in <code>messages</code>.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.Refinement","title":"Refinement","text":"<p>               Bases: <code>BaseModel</code></p> <p>Polymorphic refinement envelope for <code>refine_component</code>.</p> <p>Profiles subclass (BI: filters/drill/measure swap). Core treats as opaque.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.SessionContext","title":"SessionContext","text":"<p>               Bases: <code>BaseModel</code></p> <p>Carries user identity + permissions + BI auth token; the RLS passthrough vehicle.</p> <p><code>auth_token</code> is the user's BI token for Option B backend-fetch (the orchestrator calls the adapter on the user's behalf). Transported via HTTP <code>Authorization</code> header (extracted by the FastAPI layer), NOT the request body. It is <code>SecretStr</code> with <code>exclude=True</code> so it is never serialized into <code>model_dump()</code> / JSON / logs (\u00a710: short-lived, not logged/cached, transport-encrypted, scoped to the workspace). Adapters read it via <code>ctx.auth_token.get_secret_value()</code>.</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.TableArtifact","title":"TableArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A tabular artifact (columns + rows).</p>"},{"location":"reference/agentkit_core/models/#agentkit_core.models.TextArtifact","title":"TextArtifact","text":"<p>               Bases: <code>Artifact</code></p> <p>A plain-text artifact.</p>"},{"location":"reference/agentkit_core/protocols/","title":"protocols","text":""},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols","title":"agentkit_core.protocols","text":"<p>Protocols (structural contracts) for the adapter, orchestrator, and LLM client.</p> <p>These are the abstract seams of the framework. M1 freezes the Protocol shapes; concrete implementations arrive in later milestones:   - ComponentAdapter impls: BI adapters (M3), DCC adapters (M8+), MockAdapter (M2).   - Orchestrator impl: StatelessOrchestrator (M2, agentkit-runtime).   - LlmClient impl: LangChainLlmClient (M2, agentkit-llm-langchain).</p>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter","title":"ComponentAdapter","text":"<p>               Bases: <code>Protocol</code></p> <p>Narrow waist \u2460: translates a host tool's native objects into agentkit Components.</p> <p>Implementations live in adapter packages (agentkit-adapter-superset, ...). A runtime discovers adapters via the <code>agentkit.adapters</code> entry-point group and never statically depends on them (\u00a7C.1). <code>origin</code> + <code>capabilities</code> are instance attributes; the methods map 1:1 to the backend-sync verbs (\u00a7D.4).</p>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter.get_component","title":"get_component  <code>async</code>","text":"<pre><code>get_component(ctx: SessionContext, component_id: str) -&gt; Component\n</code></pre> <p>Return a single component by id.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_component(self, ctx: SessionContext, component_id: str) -&gt; Component:\n    \"\"\"Return a single component by id.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter.get_component_data","title":"get_component_data  <code>async</code>","text":"<pre><code>get_component_data(ctx: SessionContext, component: Component, input_args: dict[str, Any]) -&gt; ComponentData\n</code></pre> <p>Fetch data for a component (verb: get_component_data). RLS via ctx.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_component_data(\n    self, ctx: SessionContext, component: Component, input_args: dict[str, Any]\n) -&gt; ComponentData:\n    \"\"\"Fetch data for a component (verb: get_component_data). RLS via ctx.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter.get_semantic_model","title":"get_semantic_model  <code>async</code>","text":"<pre><code>get_semantic_model(ctx: SessionContext, component: Component) -&gt; ComponentSchema\n</code></pre> <p>Fetch the semantic model (schema) of a component (verb: get_semantic_model).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def get_semantic_model(self, ctx: SessionContext, component: Component) -&gt; ComponentSchema:\n    \"\"\"Fetch the semantic model (schema) of a component (verb: get_semantic_model).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter.list_components","title":"list_components  <code>async</code>","text":"<pre><code>list_components(ctx: SessionContext) -&gt; list[Component]\n</code></pre> <p>Return the catalog of available components (verb: get_catalog).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def list_components(self, ctx: SessionContext) -&gt; list[Component]:\n    \"\"\"Return the catalog of available components (verb: get_catalog).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.ComponentAdapter.refine_component","title":"refine_component  <code>async</code>","text":"<pre><code>refine_component(ctx: SessionContext, component: Component, refinement: Refinement) -&gt; ComponentData\n</code></pre> <p>Refine an already-fetched component via its semantic model (verb: refine_component).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def refine_component(\n    self, ctx: SessionContext, component: Component, refinement: Refinement\n) -&gt; ComponentData:\n    \"\"\"Refine an already-fetched component via its semantic model (verb: refine_component).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.LlmClient","title":"LlmClient","text":"<p>               Bases: <code>Protocol</code></p> <p>LLM call layer. Providers swap via env; agent code is provider-agnostic (\u00a76.4).</p> Two methods map to the Option B two-phase loop <ul> <li><code>call</code> (Phase 1, non-streaming, with tools): returns a <code>LlmResponse</code> whose   <code>tool_calls</code> the orchestrator routes (backend sync vs frontend FunctionCall).</li> <li><code>stream</code> (Phase 2, streaming, usually without tools): yields <code>LlmChunk</code>   text deltas for the final answer.</li> </ul> <p>The M2 default impl (agentkit-llm-langchain) wraps a langchain ChatModel and converts between these provider-neutral types and langchain's own.</p>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.LlmClient.call","title":"call  <code>async</code>","text":"<pre><code>call(messages: list[Any], tools: list[ToolDef] | None = None) -&gt; LlmResponse\n</code></pre> <p>Return a full response (Phase 1: with tools to obtain tool_calls).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>async def call(\n    self,\n    messages: list[Any],\n    tools: list[ToolDef] | None = None,\n) -&gt; LlmResponse:\n    \"\"\"Return a full response (Phase 1: with tools to obtain tool_calls).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.LlmClient.stream","title":"stream","text":"<pre><code>stream(messages: list[Any], tools: list[ToolDef] | None = None) -&gt; AsyncIterator[LlmChunk]\n</code></pre> <p>Yield streamed text chunks (async generator). Phase 2 final answer.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>def stream(\n    self,\n    messages: list[Any],\n    tools: list[ToolDef] | None = None,\n) -&gt; AsyncIterator[LlmChunk]:\n    \"\"\"Yield streamed text chunks (async generator). Phase 2 final answer.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.Orchestrator","title":"Orchestrator","text":"<p>               Bases: <code>Protocol</code></p> <p>Runs a query and yields the SSE stream. Stateless: state lives in request.messages.</p> <p><code>run</code> is an async generator: calling it returns an <code>AsyncIterator[BaseSSE]</code> (not a coroutine). The M2 impl (StatelessOrchestrator) follows the Option B core loop (\u00a76.1): backend-sync fetch within the request, streaming chunks/artifacts.</p>"},{"location":"reference/agentkit_core/protocols/#agentkit_core.protocols.Orchestrator.run","title":"run","text":"<pre><code>run(request: QueryRequest) -&gt; AsyncIterator[BaseSSE]\n</code></pre> <p>Yield the SSE stream for a query (async generator, not a coroutine).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\protocols.py</code> <pre><code>def run(self, request: QueryRequest) -&gt; AsyncIterator[BaseSSE]:\n    \"\"\"Yield the SSE stream for a query (async generator, not a coroutine).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/agentkit_core/testing/","title":"testing","text":""},{"location":"reference/agentkit_core/testing/#agentkit_core.testing","title":"agentkit_core.testing","text":"<p>Contract-testing DSL for the two narrow waists.</p> <p><code>CopilotResponse</code> collects an SSE stream and offers OpenBB-CopilotResponse-style assertions. M1 ships the DSL + builder helpers; the real StatelessOrchestrator arrives in M2, but the DSL is self-tested against a fake async-generator orchestrator.</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse","title":"CopilotResponse","text":"<pre><code>CopilotResponse(events: list[BaseSSE])\n</code></pre> <p>Collected SSE stream with OpenBB-style assertion helpers.</p> <p>Store the raw event list (copied).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def __init__(self, events: list[BaseSSE]) -&gt; None:\n    \"\"\"Store the raw event list (copied).\"\"\"\n    self.events: list[BaseSSE] = list(events)\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.artifacts","title":"artifacts  <code>property</code>","text":"<pre><code>artifacts: list[Artifact]\n</code></pre> <p>All artifacts carried by copilotMessageArtifact events.</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.citations","title":"citations  <code>property</code>","text":"<pre><code>citations: list[Citation]\n</code></pre> <p>All citations across copilotCitationCollection events.</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.function_calls","title":"function_calls  <code>property</code>","text":"<pre><code>function_calls: list[CopilotFunctionCall]\n</code></pre> <p>All copilotFunctionCall events (frontend UI actions requested).</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.suggestions","title":"suggestions  <code>property</code>","text":"<pre><code>suggestions: list[str]\n</code></pre> <p>All follow-up suggestions across copilotPromptSuggestions events.</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.text","title":"text  <code>property</code>","text":"<pre><code>text: str\n</code></pre> <p>Concatenated text of all copilotMessageChunk events.</p>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Concise debug representation.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Concise debug representation.\"\"\"\n    return (\n        f\"CopilotResponse(events={len(self.events)}, \"\n        f\"text={self.text!r}, artifacts={len(self.artifacts)}, \"\n        f\"function_calls={len(self.function_calls)})\"\n    )\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.has_artifact","title":"has_artifact","text":"<pre><code>has_artifact(kind: str | None = None) -&gt; bool\n</code></pre> <p>True if any artifact was emitted; if <code>kind</code> given, match that kind.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_artifact(self, kind: str | None = None) -&gt; bool:\n    \"\"\"True if any artifact was emitted; if ``kind`` given, match that kind.\"\"\"\n    if kind is None:\n        return len(self.artifacts) &gt; 0\n    return any(a.kind == kind for a in self.artifacts)\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.has_function_call","title":"has_function_call","text":"<pre><code>has_function_call(name: str | None = None) -&gt; bool\n</code></pre> <p>True if any function call was emitted; if <code>name</code> given, match that verb.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_function_call(self, name: str | None = None) -&gt; bool:\n    \"\"\"True if any function call was emitted; if ``name`` given, match that verb.\"\"\"\n    if name is None:\n        return len(self.function_calls) &gt; 0\n    return any(fc.name == name for fc in self.function_calls)\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.CopilotResponse.has_text_containing","title":"has_text_containing","text":"<pre><code>has_text_containing(needle: str) -&gt; bool\n</code></pre> <p>True if the concatenated reply text contains <code>needle</code>.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def has_text_containing(self, needle: str) -&gt; bool:\n    \"\"\"True if the concatenated reply text contains ``needle``.\"\"\"\n    return needle in self.text\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.collect_stream","title":"collect_stream  <code>async</code>","text":"<pre><code>collect_stream(stream: AsyncIterator[BaseSSE]) -&gt; CopilotResponse\n</code></pre> <p>Drain an SSE async iterator into a CopilotResponse.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>async def collect_stream(stream: AsyncIterator[BaseSSE]) -&gt; CopilotResponse:\n    \"\"\"Drain an SSE async iterator into a CopilotResponse.\"\"\"\n    events: list[BaseSSE] = []\n    async for event in stream:\n        events.append(event)\n    return CopilotResponse(events)\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.human_message","title":"human_message","text":"<pre><code>human_message(text: str) -&gt; Message\n</code></pre> <p>Build a role=human message.</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def human_message(text: str) -&gt; Message:\n    \"\"\"Build a role=human message.\"\"\"\n    return Message(role=\"human\", content=text)\n</code></pre>"},{"location":"reference/agentkit_core/testing/#agentkit_core.testing.query","title":"query","text":"<pre><code>query(text: str, *, user_identity: str = 'tester', workspace_id: str = 'ws-test', trace_id: str = 'trace-test', user_permissions: list[str] | None = None, history: list[Message] | None = None, **kwargs: Any) -&gt; QueryRequest\n</code></pre> <p>Build a minimal QueryRequest for tests.</p> <p>Extra keyword args forward to QueryRequest (tools, features, workspace_options, ...).</p> Source code in <code>packages\\agentkit-core\\src\\agentkit_core\\testing.py</code> <pre><code>def query(\n    text: str,\n    *,\n    user_identity: str = \"tester\",\n    workspace_id: str = \"ws-test\",\n    trace_id: str = \"trace-test\",\n    user_permissions: list[str] | None = None,\n    history: list[Message] | None = None,\n    **kwargs: Any,\n) -&gt; QueryRequest:\n    \"\"\"Build a minimal QueryRequest for tests.\n\n    Extra keyword args forward to QueryRequest (tools, features, workspace_options, ...).\n    \"\"\"\n    messages = list(history or [])\n    messages.append(human_message(text))\n    return QueryRequest(\n        messages=messages,\n        session_context=SessionContext(\n            user_identity=user_identity,\n            user_permissions=user_permissions or [],\n            workspace_id=workspace_id,\n            trace_id=trace_id,\n        ),\n        **kwargs,\n    )\n</code></pre>"},{"location":"reference/agentkit_core/verbs/","title":"verbs","text":""},{"location":"reference/agentkit_core/verbs/#agentkit_core.verbs","title":"agentkit_core.verbs","text":"<p>Standard verb catalogue (Option B: backend sync vs frontend FunctionCall).</p> <p>A Verb is a named action the LLM may request. Per Option B (\u00a74.2.2 / \u00a7D.4), verbs split by execution location:   * backend  - orchestrator executes synchronously (adapter method / skill registry /                MCP gateway); result returns to the LLM within the same request (multi-hop).   * frontend - orchestrator yields a FunctionCallSSE; the frontend executes and re-POSTs                a role=tool result. Reserved for UI actions only.</p> <p>M1 freezes the verb catalogue (names + input schemas + category). The live VerbRegistry (binding handlers) is wired in M2 (agentkit-runtime).</p>"},{"location":"reference/agentkit_core/verbs/#agentkit_core.verbs.VerbSpec","title":"VerbSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>Declarative verb definition: name, category, and JSON-schema input.</p>"}]}